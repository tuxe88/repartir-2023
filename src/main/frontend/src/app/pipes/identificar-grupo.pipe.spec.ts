import { IdentificarGrupoPipe } from './identificar-grupo.pipe';

describe('IdentificarGrupoPipe', () => {
  it('create an instance', () => {
    const pipe = new IdentificarGrupoPipe();
    expect(pipe).toBeTruthy();
  });
});
